# DONGJIN 第二部分开发计划：离线场景、量测白箱化、GNN数据集与模型训练

## 1. 第一部分状态与第二部分准入条件

### 1.1 当前结论

第一部分的数据底层已经完成主要功能开发：

```text
SimBench 标准电网与时间曲线
              ↓
      pandapower 加载、校验、潮流计算
              ├────────→ Neo4j：静态设备、参数和拓扑关系
              └────────→ Redis：当前完整动态潮流快照
```

已完成的验证：

- P1 权威电网包可重复生成、重新加载并通过基准潮流。
- P2 Neo4j 静态投影通过隔离数据库的完整性、幂等性、版本切换和数据保护测试。
- P3 SimBench 曲线支持 `hold` 和 `linear`，273 个动态设备均可追溯到具体曲线。
- P4 连续潮流支持启动、暂停、继续、停止和失败保护。
- P5 Redis 完整快照、TTL、活动指针和失败不切换测试通过。
- 第一部分自动化测试结果为 13 项普通测试、3 项真实 Neo4j/Redis 集成测试通过。
- 长时测试停止时已连续运行约 10 分钟、591 步，无错误，Redis 快照数量稳定在 60 份。

当前未完成的正式验收项：

- 第一部分计划中的 30 分钟连续稳定性测试已按要求暂停。
- Memurai已使用项目E盘运行目录完成本机`6379`读写和Python健康检查；Neo4j仍需现场启动确认，且两库与实际P1发布/连续潮流的完整现场联调仍未执行。

因此当前状态应表述为：

> 数据底层已经具备继续开发第二部分的能力，但第一部分“生产式完全验收”仍保留长时测试和在线分支现场联调两个尾项；这两个尾项不阻塞只依赖P1包的离线训练分支。

### 1.2 第二部分实施准入

第二部分离线训练分支的准入只依赖P1权威电网包，不要求Neo4j、Redis或在线
连续潮流正在运行。开始批量造数和训练前必须确认：

1. 当前活动 P1 电网包可正常读取。
2. P1 Manifest、network、topology、ID mapping和SimBench曲线校验通过。
3. 所有离线图节点和事件目标继续使用P1稳定`businessId`，不重新编号。
4. 第一部分未完成的验收项被明确记录，不把“测试暂缓”描述为“已经通过”。

Neo4j和Redis属于另一条在线运行分支。它们可以在第二部分开发期间关闭；如果处于
运行状态，第二部分只能执行只读一致性检查，任何离线场景和训练任务都不得切换
活动图、发布活动快照或把在线数据当作训练历史。

---

## 2. 第二部分目标

第二部分是独立于正常在线运行时钟的离线训练分支。它不再生成另一套随机电网，
而是以第一部分冻结的P1权威电网包为共同原本，在独立pandapower副本上批量模拟
场景、生成标准数据集，并在数据集验收后手动启动GNN训练，直到产出可供后续
在线研判使用的合格模型。

```text
                     P1 权威电网包
                           │
          ┌────────────────┴────────────────┐
          │                                 │
离线训练分支（本部分，先完成）       在线运行分支（正式使用）
          │                                 │
ScenarioDefinition                     Neo4j静态拓扑
          ↓                                 │
pandapower独立场景副本                 pandapower正常潮流
          ↓                                 ↓
TruthSnapshot                         Redis当前动态快照
          ↓                                 ↓
MeasurementFrame                     后续在线错误研判
          ↓                                 ↑
ScenarioArtifact                       合格模型
          ↓                                 │
GNN Dataset → 手动训练 → 评估验收 ──────────┘
```

这里的“异步”是指离线训练分支拥有独立任务、独立仿真时钟和独立产物目录：
它可以比在线运行更快地批量生成大量场景，也可以暂停、重跑或并行分批执行，
但不得读取在线Redis作为训练历史，不得向在线Redis注入模拟故障，不得修改
Neo4j活动拓扑，也不得阻塞正常在线潮流。

本部分出现的所有“故障”“错误”和“异常”都是为了训练而构造的离线模拟场景，
不代表在线运行分支真的发生了故障。它们只存在于场景档案、数据集和训练产物中。
以后在线分支出现的实时异常或演示用隐藏故障是另一类运行期事件，不由本部分
场景执行器直接驱动。

完成后，系统应当能够：

1. 在指定 SimBench 电网和时间点上定义可重复的运行场景。
2. 使用 pandapower 计算场景造成的物理影响，而不是直接编造电压、电流和负载率。
3. 明确区分物理真实值、传感器观测值、质量问题和故障标签。
4. 对噪声、偏置、延迟、丢失、卡死和量化进行可配置仿真。
5. 把完整场景保存为可校验、可重放、可构建数据集的版本化产物。
6. 生成与 Neo4j 稳定业务 ID 对齐的 GNN 图样本。
7. 通过现有Python服务的命令或后端接口批量生成数据，不建立第二套项目。
8. 使用标准数据集手动训练、验证和测试GNN，保存可追溯模型产物。
9. 用明确验收门槛判定模型是否可以交给后续在线研判，禁止未训练或不兼容模型进入正式使用。
10. 保证离线训练任务和在线Neo4j、Redis、pandapower运行链路相互隔离。

---

## 3. 第二部分范围

### 3.1 包含内容

- 场景定义、校验、版本和随机种子。
- 负荷、分布式电源、开关、线路和变压器运行事件。
- 线路/变压器停运、开关分合、负荷阶跃和新能源出力变化。
- 基于 AC 稳态潮流的过载、欠压、过压、孤岛和供电中断结果。
- 短路计算作为独立的一次性分析类型，不伪装成连续稳态潮流。
- 物理真实值与量测值的双层数据模型。
- 量测噪声、偏置、延迟、丢失、冻结、量化和质量码。
- 场景完整档案和面向机器学习的 Parquet 数据集。
- 图特征、标签、掩码、时间窗口和数据集切分。
- 可配置批量场景矩阵和数据量、类别、目标设备、时间区间覆盖报告。
- 通过现有后端接口或命令手动启动GNN训练、评估和模型产物冻结。
- 模型与数据集、P1拓扑版本、特征Schema和归一化参数的兼容检查。
- 必要的Java管理接口复用；正常运行前端不增加训练模块。
- 现有随机故障样本链路的迁移、废弃标记和冗余清理。
- 第二部分自动化测试、离线服务集成、训练验收和分支隔离测试。

### 3.2 暂不包含

- 前端训练按钮和训练进度页面。
- 在线运行或数据集生成完成后自动触发GNN训练。
- 在线推理、模型激活、灰度发布和模型回滚。
- 电磁暂态、毫秒级录波、继电保护整定和保护动作时序。
- 将插值得到的数据描述成真实 SCADA 秒级采样。
- 真实生产电网文件、真实调度指令和真实用户隐私数据。
- CIM/CGMES 导入。
- Kafka 和大规模分布式流处理。
- TimescaleDB、TDengine 等在线长周期查询数据库。
- 多电网并行仿真和集群调度。

上述内容需要在第二部分验收后单独制定后续计划。

---

## 4. 已确定的技术决策

| 项目 | 第二部分决定 |
|---|---|
| 权威静态来源 | 继续使用第一部分版本化 P1 电网包 |
| 离线图结构来源 | 直接读取P1 topology与ID mapping，不依赖Neo4j在线状态 |
| 在线静态关系 | Neo4j保留给在线分支；离线任务最多进行只读一致性检查 |
| 在线动态状态 | Redis保留给在线分支，不作为离线训练输入或场景档案 |
| 场景计算 | 在活动 `network.json` 的独立副本上执行，不直接修改权威包 |
| 稳态场景 | pandapower AC `runpp()` |
| 短路场景 | 使用 pandapower short-circuit 模块单独计算并使用独立 Schema |
| 场景定义 | JSON + Pydantic，版本化保存 |
| 场景档案 | Manifest + JSON 元数据 + Parquet 帧数据 |
| 离线数据集 | Parquet，按 `datasetId/scenarioId` 分区 |
| 当前快照 | Redis 仍只保存当前及短期快照，不承担训练集存储 |
| 分支隔离 | 离线场景使用独立网络副本、仿真时钟和产物目录，不读写在线活动快照 |
| 长期在线时序库 | 第二部分不引入；未来按查询需求评估 |
| 随机性 | 所有噪声、故障目标和参数采样必须使用显式 `randomSeed` |
| 前端边界 | 正常运行前端不放训练或离线故障注入入口；如需管理接口仍经Spring Boot 8080 |
| Python 服务 | 继续扩展现有 `python-training-service/app/grid`，不新建端口和项目 |
| 训练方式 | 数据集验收后由明确命令或现有后端接口手动启动，不由前端或在线流程自动触发 |
| 模型产物 | 保存模型权重、Manifest、指标、数据集ID、拓扑签名、特征Schema和校验和 |
| 正式使用门槛 | 只有训练评估通过且与活动P1版本兼容的模型，才允许进入后续在线研判 |

---

## 5. 三层数据白箱模型

第二部分必须同时保留三层对象，禁止混为一谈。

### 5.1 场景与根因：`ScenarioDefinition`

描述系统主动施加了什么事件：

- 哪个电网和拓扑版本。
- 场景起止时间。
- 事件目标和稳定业务 ID。
- 事件类型、参数、开始时间和持续时间。
- 随机种子。
- 是否允许恢复。
- 预期计算类型。

### 5.2 物理真实值：`TruthSnapshot`

由 pandapower 计算产生：

- Bus 电压、相角、P/Q。
- Line 两端 P/Q、电流、负载率和损耗。
- Transformer 两侧功率、电流、负载率和档位。
- Switch 实际分合状态。
- Load、SGen、ExternalGrid 实际输入或平衡结果。
- 潮流是否收敛。
- 事件根因、事件阶段和受影响对象。

`TruthSnapshot` 是场景内部的物理真值，不等于真实生产系统的现场真值。

### 5.3 可见量测：`MeasurementFrame`

由 `TruthSnapshot` 经量测模型转换产生：

- 量测值。
- 量测时间和到达时间。
- 来源 TruthSnapshot。
- 噪声、偏置和量化参数。
- 是否延迟、丢失、冻结或替代。
- 质量码。
- 原始值、输出值和变换过程。

原则：

> GNN 输入使用 MeasurementFrame；标签和物理核对使用 TruthSnapshot；根因使用 ScenarioDefinition。

---

## 6. 统一身份、时间与版本

所有第二部分产物必须包含：

| 字段 | 含义 | 示例 |
|---|---|---|
| `gridId` | 电网身份 | `simbench-1-mv-urban-0-sw` |
| `topologyVersion` | 静态拓扑版本 | `v1` |
| `scenarioId` | 场景身份 | `scenario-line-outage-0001` |
| `scenarioRunId` | 单次执行身份 | `run-20260723-000001` |
| `scenarioSchemaVersion` | 场景协议版本 | `grid-scenario-v1` |
| `truthSchemaVersion` | 物理真值协议 | `grid-truth-v1` |
| `measurementSchemaVersion` | 量测协议 | `grid-measurement-v1` |
| `datasetSchemaVersion` | 数据集协议 | `grid-gnn-dataset-v1` |
| `eventId` | 单个事件身份 | `event-line-42-open` |
| `frameId` | 单帧身份 | `run-...-frame-000123` |
| `simulationTime` | 物理仿真时间 | ISO 8601 |
| `measurementTime` | 量测对应时间 | ISO 8601 |
| `arrivalTime` | 量测到达时间 | ISO 8601 |
| `generatedAt` | 产物生成时间 | ISO 8601 |
| `randomSeed` | 可复现随机种子 | `20260723` |

设备身份继续使用：

```text
{gridId}:{elementType}:{sourceIndex}
```

不允许在场景、量测或数据集中重新编号设备。

---

## 7. 场景分类

### 7.1 正常运行场景

- 工作日/周末负荷曲线。
- 高峰、低谷和新能源高出力。
- 正常开关组合。
- 正常电压和负载率波动。

正常样本必须作为明确类别存在，避免模型把任何状态都判断成故障。

### 7.2 运行异常场景

- 负荷阶跃上升或下降。
- 新能源出力骤降、限发或恢复。
- 线路停运。
- 变压器停运。
- 开关误分或误合。
- 变压器档位异常。
- 局部孤岛。
- 供电中断。
- 线路或变压器过载。
- 母线欠压或过压。

### 7.3 短路分析场景

- 三相短路。
- 两相短路。
- 单相接地短路。

短路结果必须使用独立 `grid-short-circuit-v1`，不得直接塞入普通 `grid-truth-v1` 连续潮流帧。

### 7.4 量测异常场景

- 高斯噪声。
- 固定偏置。
- 漂移。
- 卡死值。
- 数据丢失。
- 到达延迟。
- 乱序。
- 量化。
- 单位或变比配置错误。

量测异常不应自动改动物理真值，必须能识别“电网正常但传感器异常”。

---

## 8. 模块 S0：第一部分契约冻结

**目标**：在第二部分修改代码前，冻结第一部分作为上游数据契约。

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S0.1 | 冻结 P1 清单 | 明确第二部分读取的 manifest、network、topology、ID mapping 和 profile 元数据字段 |
| S0.2 | 冻结离线图身份 | 明确P1拓扑、设备类型和稳定businessId，离线数据不得重新编号 |
| S0.3 | 冻结在线隔离边界 | 明确Neo4j活动模型和Redis活动Key只属于在线分支，离线任务禁止写入 |
| S0.4 | 第一部分准入检查 | 新增只读检查命令确认P1文件、Schema、校验和和基准潮流；Neo4j/Redis检查为可选 |
| S0.5 | 兼容策略 | 第一部分 Schema 变化时第二部分必须明确拒绝或升级，禁止静默兼容 |

第二部分不得修改 P1 电网包内容，也不得把场景结果写回 `network.json`。

---

## 9. 模块 S1：场景定义与校验

**目标**：建立可版本化、可复现和可审计的场景语言。

### 9.1 `grid-scenario-v1`

```json
{
  "scenarioId": "scenario-line-outage-0001",
  "scenarioSchemaVersion": "grid-scenario-v1",
  "gridId": "simbench-1-mv-urban-0-sw",
  "topologyVersion": "v1",
  "name": "线路停运与负荷恢复",
  "randomSeed": 20260723,
  "startTime": "2016-01-01T08:00:00Z",
  "durationSeconds": 900,
  "events": [
    {
      "eventId": "event-line-42-out",
      "eventType": "LINE_OUT_OF_SERVICE",
      "targetBusinessId": "simbench-1-mv-urban-0-sw:line:42",
      "offsetSeconds": 120,
      "durationSeconds": 300,
      "parameters": {}
    }
  ]
}
```

### 9.2 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S1.1 | 定义 Pydantic Schema | 所有字段、枚举、单位和范围明确 |
| S1.2 | 目标校验 | 目标 businessId 必须属于指定 P1 包且类型匹配 |
| S1.3 | 时间校验 | 事件不得越过场景范围；重叠事件必须有明确冲突规则 |
| S1.4 | 参数校验 | 负荷比例、出力比例、档位和开关状态符合设备能力 |
| S1.5 | 可复现随机性 | 同一场景和 randomSeed 产生相同事件参数 |
| S1.6 | 场景模板 | 提供正常、过载、欠压、线路停运、开关误动和量测异常模板 |
| S1.7 | 场景清单 | Manifest 记录来源、版本、创建时间和校验和 |

---

## 10. 模块 S2：物理场景执行器

**目标**：在 P1 网络副本上执行事件并生成物理一致的 TruthSnapshot。

### 10.1 执行原则

1. 每个 `scenarioRunId` 加载独立 pandapowerNet 副本。
2. 使用 SimBench 曲线生成该时刻正常基线。
3. 按事件时间线修改副本。
4. 执行 `runpp()`。
5. 校验收敛、有限数值和设备覆盖。
6. 生成 TruthSnapshot。
7. 场景结束后丢弃副本，不改写 P1 权威包。

### 10.2 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S2.1 | 网络副本管理 | 场景之间不共享可变 pandapowerNet |
| S2.2 | 事件调度器 | 支持开始、持续、恢复和同时间多事件 |
| S2.3 | 负荷/出力事件 | 在正常曲线值基础上施加变化，不覆盖曲线来源信息 |
| S2.4 | 拓扑运行事件 | 支持开关、线路和变压器 `in_service/closed` 变化 |
| S2.5 | 潮流结果和标签 | 根因、事件阶段、阈值越限和受影响对象全部记录 |
| S2.6 | 不收敛分类 | 区分预期不可供电、数值不收敛和配置错误 |
| S2.7 | 短路适配器 | 独立调用短路计算并输出专用结果 |
| S2.8 | 基线对照 | 每个异常场景保留同时间、同曲线、无事件的基线结果 |

禁止通过直接修改 `res_bus`、`res_line` 等结果表制造故障结果。

---

## 11. 模块 S3：量测与质量模型

**目标**：从物理真值生成可解释的模拟量测。

### 11.1 量测点

| 对象 | 第一版量测 |
|---|---|
| Bus | `vmPu`、`vaDegree`、`pMw`、`qMvar` |
| Line | 两端 P/Q、电流、负载率 |
| Transformer | 两侧 P/Q、电流、负载率、档位 |
| Switch | 分合状态、是否投入运行 |
| Load | 当前 P/Q |
| SGen | 当前 P/Q |
| ExternalGrid | 平衡 P/Q |

### 11.2 质量码

第一版统一质量码：

- `GOOD`
- `SUSPECT`
- `BAD`
- `MISSING`
- `SUBSTITUTED`
- `DELAYED`
- `FROZEN`

### 11.3 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S3.1 | 量测设备配置 | 可按设备类型和单个 businessId 配置采样规则 |
| S3.2 | 噪声模型 | 支持绝对误差和相对误差，记录分布和参数 |
| S3.3 | 偏置与漂移 | 记录开始时间、偏置量和漂移速率 |
| S3.4 | 丢失与卡死 | 丢失不自动补零；卡死保留来源帧 |
| S3.5 | 延迟与乱序 | 区分 measurementTime 和 arrivalTime |
| S3.6 | 量化 | 可配置分辨率和舍入规则 |
| S3.7 | 质量码 | 每个字段或量测对象具有明确质量状态 |
| S3.8 | 变换审计 | 可从输出量测追溯到 TruthSnapshot 和全部变换 |

所有量测异常都是模拟结果，文档和界面必须使用“模拟量测”，不得描述为真实 SCADA 数据。

---

## 12. 模块 S4：场景档案与长期离线存储

**目标**：把场景保存为可重放、可校验、可构建数据集的离线产物。

### 12.1 目录结构

```text
python-training-service/artifacts/scenarios/
└── {scenarioId}/
    └── {scenarioRunId}/
        ├── manifest.json
        ├── scenario.json
        ├── baseline-summary.json
        ├── truth/
        │   └── frames.parquet
        ├── measurements/
        │   └── frames.parquet
        ├── events.parquet
        ├── labels.parquet
        └── validation.json
```

### 12.2 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S4.1 | 场景 Manifest | 记录全部 Schema、版本、种子、帧数、依赖和校验和 |
| S4.2 | 原子完成 | 临时目录生成并校验后再切换为完成状态 |
| S4.3 | Parquet Schema | 字段类型和单位固定，不依赖 DataFrame 自动推断 |
| S4.4 | 真值和量测分离 | Truth、Measurement、Event、Label 独立保存 |
| S4.5 | 分区和压缩 | 按运行分区，使用统一压缩算法 |
| S4.6 | 重放 | 可从 scenario.json 和种子重新运行并核对核心结果 |
| S4.7 | 保留策略 | 明确临时失败产物、完成产物和手工删除流程 |

Redis 不用于保存完整训练历史，Neo4j 不用于写入每个时间帧。

---

## 13. 模块 S5：GNN 标准数据集构建

**目标**：把场景档案转换为训练可用、无标签泄漏的图时序数据集。

### 13.1 数据集结构

```text
artifacts/datasets/{datasetId}/
├── manifest.json
├── graph.json
├── feature-schema.json
├── label-schema.json
├── normalization.json
├── train.parquet
├── validation.parquet
├── test.parquet
└── source-scenarios.json
```

### 13.2 图对象

- 图节点和边使用 P1 稳定业务 ID。
- Line、Transformer 和 Switch 是否作为图顶点必须在 `graph.json` 固定。
- 拓扑结构来自指定 `topologyVersion`，不从量测数据反推连接。
- 开关运行状态可作为动态特征，但不能改变设备身份。

### 13.3 特征与标签

第一版候选特征：

- 电压、相角、P/Q、电流和负载率。
- 与基线的差值和变化率。
- 质量码和缺失掩码。
- 设备类型、额定值和拓扑度。
- 时间窗口内的均值、标准差、最大值和斜率。

第一版标签：

- `NORMAL`
- `LINE_OUTAGE`
- `TRANSFORMER_OUTAGE`
- `SWITCH_MISOPERATION`
- `LOAD_SURGE`
- `GENERATION_DROP`
- `OVERLOAD`
- `UNDERVOLTAGE`
- `OVERVOLTAGE`
- `ISLANDING`
- `MEASUREMENT_BIAS`
- `MEASUREMENT_DROPOUT`
- `MEASUREMENT_FROZEN`

物理故障标签和量测质量标签必须允许同时存在，不能强制合并成单一类别。

### 13.4 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S5.1 | 特征 Schema | 字段、单位、数据类型、缺失策略和归一化方式明确 |
| S5.2 | 标签 Schema | 根因、位置、事件阶段、影响范围和量测异常分层 |
| S5.3 | 时间窗口 | 窗口长度、步长、前后文和边界填充明确 |
| S5.4 | 缺失掩码 | 缺失值与真实零值可区分 |
| S5.5 | 数据集切分 | 按 scenarioRunId 切分，禁止同一场景帧跨训练/测试集泄漏 |
| S5.6 | 类别分布 | 输出正常/异常、根因类型和目标设备分布 |
| S5.7 | 拓扑签名 | 数据集记录 gridId、topologyVersion 和图签名 |
| S5.8 | 数据集可复现 | 同一场景集合和配置生成相同核心文件校验和 |
| S5.9 | 手动训练适配 | 提供读取器接入离线`/offline/training/run`流程 |
| S5.10 | 数据规模与覆盖 | 可配置各类别、目标设备和时间区间的场景数；Manifest输出实际样本量和覆盖缺口 |

---

## 14. 模块 S6：GNN离线训练与模型验收

**目标**：在数据集验收后显式启动训练，完成验证、测试和模型冻结，为后续在线
研判提供与P1版本严格兼容的合格模型。

训练输入固定为：

- `grid-gnn-dataset-v1`数据集及其Manifest。
- P1图签名、稳定业务ID和指定`topologyVersion`。
- 特征、标签、掩码、归一化参数和数据集切分。
- 显式训练配置、超参数和随机种子。

训练输出固定为：

```text
artifacts/models/{modelId}/
├── manifest.json
├── model.*
├── metrics.json
├── training-config.json
├── feature-schema.json
├── label-schema.json
├── normalization.json
└── validation.json
```

### 14.1 任务

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S6.1 | 数据读取适配 | 现有GNN训练流程可读取S5数据集，不再读取旧随机特征样本 |
| S6.2 | 显式训练入口 | 仅通过命令或现有后端接口手动启动；数据集构建和在线运行均不会自动触发 |
| S6.3 | 训练配置冻结 | 记录模型结构、超参数、随机种子、代码版本和依赖版本 |
| S6.4 | 评估指标 | 输出分类F1、正常/异常识别、故障位置Top-K、混淆矩阵和各类别支持数 |
| S6.5 | 独立测试集 | 仅在模型选择完成后评估测试集，禁止用测试集调参 |
| S6.6 | 可复现检查 | 同一数据集、配置和种子重复训练的指标在约定容差内一致 |
| S6.7 | 模型Manifest | 模型可追溯到datasetId、场景集合、P1版本、拓扑签名和全部Schema |
| S6.8 | 兼容性门禁 | 模型与活动gridId、topologyVersion、图签名或特征Schema不一致时明确拒绝 |
| S6.9 | 合格判定 | 指标门槛配置化；未达标模型保存为失败产物，不标记为可供正式使用 |
| S6.10 | 失败隔离 | 训练失败、取消或重跑不影响在线Neo4j、Redis和连续潮流 |

第二部分的“训练完成”是指：至少一个模型通过独立验证与测试、Manifest完整、
兼容检查通过，并被冻结为可交付模型产物；不等同于已经部署到在线推理。

---

## 15. 模块 S7：现有后端复用与旧代码迁移

**目标**：继续使用现有Python服务、Java网关和手动训练接口，不建立平行系统，
同时确保离线训练分支不混入正常运行前端。

### 15.1 Python 内部接口

```text
POST /scenarios/validate
POST /scenarios
POST /scenario-batches
GET  /scenario-runs/{runId}
GET  /scenario-runs/{runId}/truth/current
GET  /scenario-runs/{runId}/measurements/current
POST /datasets/build
GET  /datasets
GET  /datasets/{datasetId}
POST /training/runs
GET  /training/runs/{trainingRunId}
GET  /models/{modelId}
```

### 15.2 Java 统一代理

确需通过Java管理离线任务时，继续复用`PythonComputeGateway`和现有训练接口，
不再创建第二套Python HTTP客户端：

```text
/api/offline-scenarios/*
/api/datasets/*
/api/training/*
/api/models/*
```

命令行或Java管理接口都只操作离线产物目录；不得把模拟故障发布为在线活动快照。

### 15.3 前端边界

- 正常运行前端继续展示P1拓扑、Redis动态潮流和后续在线研判。
- 不恢复已经删除的训练模块。
- 不在正常拓扑页面提供离线故障注入、批量造数或训练按钮。
- 如未来需要离线实验可视化，应作为明确的开发/管理视图单独规划，且仍经Java调用。

### 15.4 旧代码迁移

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S7.1 | 复用 Python 服务 | 场景、数据集和训练模块继续位于现有服务 |
| S7.2 | 复用 Java 网关 | 不增加第二套 Python HTTP 客户端 |
| S7.3 | 隔离正常前端 | 正常拓扑页面没有离线造数和训练入口 |
| S7.4 | 迁移旧随机样本 | 旧 `FaultGenerationService` 标记废弃，确认无调用后删除 |
| S7.5 | 清理随机特征 | 不再把无物理来源的随机电压、电流作为正式训练数据 |
| S7.6 | 兼容手动训练 | 现有训练接口读取S5数据并产生S6模型产物 |
| S7.7 | 错误信息统一 | Python错误经Java转换为明确的4xx/5xx |
| S7.8 | 运行分支保护 | 离线任务不能切换Neo4j活动版本或Redis活动快照 |

---

## 16. 模块 S8：测试和第二部分验收

### 16.1 自动化测试

| 编号 | 测试范围 | 必测内容 |
|---|---|---|
| S8.1 | 场景 Schema | 类型、目标、单位、时间范围和冲突校验 |
| S8.2 | 可复现性 | 同一随机种子得到相同事件和量测噪声 |
| S8.3 | 物理场景 | 事件实际改变pandapower输入或拓扑，结果由runpp产生 |
| S8.4 | 基线对照 | 同时间无事件基线和异常结果可比较 |
| S8.5 | 量测模型 | 噪声、偏置、丢失、冻结、延迟和质量码 |
| S8.6 | 场景档案 | Parquet Schema、校验和、原子完成和重放 |
| S8.7 | 数据集 | ID、特征、标签、掩码、切分和无泄漏 |
| S8.8 | GNN训练 | 显式启动、可复现、指标完整、测试集隔离和失败处理 |
| S8.9 | 模型产物 | Manifest、校验和、拓扑/特征兼容门禁和合格状态 |
| S8.10 | 分支隔离 | 离线批量任务期间在线Neo4j、Redis活动状态不被修改 |
| S8.11 | Java API | 必要的离线管理接口继续复用现有网关 |
| S8.12 | 前端 | 已删除的训练模块不恢复，正常拓扑功能不受离线任务影响 |
| S8.13 | 端到端 | P1包 → 场景 → Truth → Measurement → Dataset → GNN训练 → 合格模型 |

### 16.2 最终验收标准

必须同时满足：

1. 指定 P1 电网包可以创建正常和异常场景。
2. 场景不会改写 P1 权威 `network.json`。
3. 同一场景和种子重复运行时事件、量测变换和核心结果可复现。
4. 每个 TruthSnapshot 都能追溯到场景、事件和 P1 拓扑版本。
5. 每个 MeasurementFrame 都能追溯到 TruthSnapshot 和量测变换。
6. 物理异常由 pandapower 计算产生，不直接伪造结果表。
7. 量测异常不会污染物理真值。
8. 场景档案重新加载后帧数、字段、单位和标签一致。
9. 数据集中所有对象使用与 Neo4j 相同的业务 ID。
10. 训练、验证和测试集按场景运行切分，不发生帧级泄漏。
11. 可通过配置生成满足类别、设备和时间覆盖要求的大批量数据，实际规模记录在Manifest。
12. GNN训练只能被命令或后端接口显式启动，在线运行和数据集完成不会自动触发。
13. 训练输出独立验证、测试指标和完整模型Manifest。
14. 至少一个模型达到配置的合格门槛，并与当前P1拓扑和特征Schema兼容。
15. Redis仍只承担在线当前和短期快照，不保存训练场景历史。
16. 离线模拟故障不会进入在线Redis、不会修改Neo4j活动拓扑，也不会阻塞在线潮流。
17. 正常前端不恢复训练模块，不提供离线故障注入入口。
18. 旧随机故障样本链路完成废弃或隔离，不再作为正式数据源。

---

## 17. 推荐目录

```text
python-training-service/
├── app/
│   └── grid/
│       ├── artifact_service.py
│       ├── publishers/
│       ├── simulation/
│       ├── scenarios/
│       │   ├── models.py
│       │   ├── validator.py
│       │   ├── event_engine.py
│       │   ├── truth_builder.py
│       │   ├── measurement_model.py
│       │   ├── archive.py
│       │   └── api.py
│       ├── datasets/
│       │   ├── schema.py
│       │   ├── builder.py
│       │   ├── splitter.py
│       │   └── api.py
│       └── training/
│           ├── dataset_adapter.py
│           ├── trainer.py
│           ├── evaluator.py
│           └── model_manifest.py
├── artifacts/
│   ├── grids/
│   ├── scenarios/
│   ├── datasets/
│   └── models/
└── tests/
    ├── scenarios/
    ├── datasets/
    └── training/
```

Java只在现有网关和训练模块中增加必要接口，不重新创建应用；正常前端不增加
离线训练模块。

---

## 18. 实施顺序和里程碑

```text
S0 第一部分契约冻结
       ↓
S1 场景定义与校验
       ↓
S2 物理场景执行器
       ↓
S3 量测与质量模型
       ↓
S4 场景档案
       ↓
S5 GNN标准数据集
       ↓
S6 GNN离线训练与模型验收
       ↓
S7 现有后端复用与旧代码迁移
       ↓
S8 测试和验收
```

### 里程碑 A：场景可重复

完成 S0、S1。场景定义可校验、可版本化、可重复采样。

### 里程碑 B：物理真值可生成

完成 S2。正常和异常场景均由 pandapower 生成 TruthSnapshot。

### 里程碑 C：量测白箱化

完成 S3。可以对照物理真值、模拟量测和每一步质量变换。

### 里程碑 D：场景可保存和重放

完成 S4。场景档案可校验、重载和重放。

### 里程碑 E：GNN 数据集可交付

完成 S5。能够生成无 ID 错位、无帧级泄漏的标准数据集。

### 里程碑 F：GNN模型训练完成

完成S6。通过显式命令或后端接口训练并验收至少一个合格模型；模型与P1拓扑、
数据集和特征Schema可追溯且兼容。

### 里程碑 G：第二部分正式完成

完成S7、S8。离线训练分支与在线运行分支隔离，旧随机正式数据源退出，端到端
验收通过。此时才具备进入后续在线错误研判开发和正式使用的模型前置条件。

---

## 19. 实施期间必须遵守的规则

1. 不新建第二套随机拓扑。
2. 不改写 P1 权威电网包。
3. 不直接修改 pandapower 结果表制造故障。
4. 不把 TruthSnapshot 描述成真实生产现场真值。
5. 不把模拟量测描述成真实 SCADA 数据。
6. 不把物理异常、量测异常和标签混成一个字段。
7. 不用随机 DataFrame 索引代替稳定业务 ID。
8. 不让前端绕过 Java 直连 Python。
9. 不把完整长期场景历史塞进 Redis。
10. 不把每个时间帧写入 Neo4j。
11. 不按帧随机切分训练集和测试集。
12. 不允许同一场景运行同时出现在训练集和测试集。
13. 不在场景数据集生成后或在线运行过程中自动触发GNN训练。
14. 不把离线模拟故障写入在线Redis或Neo4j活动拓扑。
15. 不让离线训练任务占用或修改在线pandapower运行实例。
16. 未训练、未达标或版本不兼容的模型不得用于正式研判。
17. 正常前端不得恢复训练模块或离线故障注入入口。
18. Schema变化必须提升版本并补充兼容说明。
19. 新链路稳定后必须删除或隔离无物理来源的旧随机样本代码。

---

## 20. 第二部分之后的后续入口

第二部分验收后，再单独制定下一部分计划。候选方向：

- 将第二部分合格模型注册、激活并接入在线MeasurementFrame。
- 模型版本切换、灰度发布和回滚。
- 在线推理消费当前 MeasurementFrame。
- 故障定位、分类、置信度、上下游影响和解释。
- 真实历史时序数据库。
- 更完整的短路和保护动作分析。
- CIM/CGMES 或合法脱敏生产数据导入。

第二部分已经完成离线训练和模型验收；上述后续入口负责把合格模型接入正式
在线运行分支，不再重新生成训练数据或默认重新训练。

---

## 21. 2026-07-23 实施结果与白箱交付

### 21.1 已完成主链路

第二部分v1主链路已经实现并完成真实端到端运行：

```text
P1权威电网包
  → 108个离线场景
  → 基线Truth + 事件Truth + Measurement + 变换审计
  → 94,176行GNN数据集
  → 两层GCN训练
  → REJECTED失败记录 + QUALIFIED合格模型
```

| 模块 | 实施状态 | 主要证据 |
|---|---|---|
| S0 | 已完成 | 只读加载P1 Manifest/network/topology/ID映射；离线模块无Neo4j/Redis发布器依赖 |
| S1 | 已完成v1 | Pydantic事件、稳定ID、版本、种子、正常类和8类异常 |
| S2 | 已完成v1 | 每个场景独立网络副本、SimBench曲线、基线与事件`runpp()`、影响标签 |
| S3 | 已完成v1 | 真值/量测分层、随机噪声、偏置、丢失、冻结、质量码和逐字段审计 |
| S4 | 已完成 | 临时目录原子落盘、Manifest、SHA-256、JSON/JSONL/CSV和人工摘要 |
| S5 | 已完成 | 稳定图、48特征、缺失掩码、场景级分层切分、CSV/JSONL/Parquet |
| S6 | 已完成 | 显式训练、两层GCN、早停、验证/测试、逐场景预测和资格门禁 |
| S7 | 已完成 | 复用现有Python服务和Java网关；旧Java随机样本链路删除；前端训练模块未恢复 |
| S8 | 已完成v1验收 | Python全量自动化测试、真实端到端测试、前端构建和Java变更编译检查 |

“已完成v1”表示当前正式模型使用本次交付的九类单帧稳态场景。任务清单中列出的
短路专用Schema、漂移、到达延迟、乱序、量化和多帧时间窗口仍属于扩展场景，
不应被误写成当前已生成数据；它们不会阻塞本次九类GCN数据链路使用，但若要求
覆盖这些类别，需要在新Schema/新数据集版本中继续实现和重新训练。

### 21.2 正式场景批次

```text
python-training-service/artifacts/scenarios/p1-v1-offline-training-v1
```

- 108个场景，9类各12个。
- 失败场景0个，生成耗时54.677秒。
- 每个场景保留872个对象的基线真值、事件真值和量测。
- 当前类别：正常、线路停运、变压器停运、开关误动、负荷突增、分布式电源
  出力下降、量测偏置、量测丢失、量测冻结。
- 共869个可见文件，约199.24MB。

### 21.3 正式GNN数据集

```text
python-training-service/artifacts/datasets/p1-v1-gnn-dataset-v1
```

- 94,176行，108个完整图场景。
- 872个顶点、1,182条图边、48个显式特征。
- 训练/验证/测试场景数为63/18/27。
- 九类各12个场景；同一`scenarioRunId`不会跨集合。
- 完整人工可读文件为`samples.csv`和`samples.jsonl`，快速查看文件为
  `preview-first-1000-rows.csv`。
- 机器训练文件为`train.parquet`、`validation.parquet`和`test.parquet`。
- 共16个文件，约242.93MB。

### 21.4 模型资格记录

```text
python-training-service/artifacts/models/p1-v1-gcn-model-v1
python-training-service/artifacts/models/p1-v1-gcn-model-v2
python-training-service/artifacts/models/p1-v1-gcn-model-v3
```

v1因正常图识别不达标被保留为`REJECTED`，没有被覆盖或伪装成成功。v2完成
训练集与验证集异常阈值校准。随后将默认资格门槛提高为Macro F1 0.70、
Top-5 0.70、精确诊断0.65、正常图识别0.80、故障召回0.75，并生成严格门槛
v3；v3状态为`QUALIFIED`，测试集结果：

| 指标 | v3结果 |
|---|---:|
| 目标类别Macro F1 | 0.866667 |
| 目标类别准确率 | 0.958333 |
| 故障位置Top-1 | 0.791667 |
| 故障位置Top-5 | 0.791667 |
| 精确诊断 | 0.750000 |
| 正常图识别率 | 1.000000 |
| 故障检测召回率 | 0.833333 |

合格只表示通过本次SimBench模拟数据的配置门槛，不代表真实现场效果，也不会
自动激活到在线研判。

### 21.5 用户可见性

所有API返回绝对`artifactPath`和关键白箱文件路径。用户不需要读取Python对象
或Java内存即可查看：

- 事件根因和参数；
- pandapower基线与事件真值；
- 模型真正接收的量测；
- 真值到量测的每一步变换；
- 每行特征与标签；
- 数据切分与归一化；
- 每轮训练历史；
- 验证/测试逐场景预测；
- 合格和不合格模型的门槛证据。

操作命令、文件树、接口和指标详见`OFFLINE_TRAINING_GUIDE.md`。

### 21.6 验证记录

- Python：`15 passed, 3 skipped`。3项跳过的是需要隔离Neo4j/Redis进程的第一部分
  在线集成测试；第二部分离线端到端测试实际执行并通过。
- 离线API契约：5个关键路径存在，旧`POST /train`随机样本入口已移除。
- 前端：`npm run build`通过，训练组件和训练服务未恢复。
- Java：使用本机现有JDK 17对`TrainingController`、`PythonComputeGateway`及相关
  DTO执行UTF-8编译检查并通过。
- 正式产物回读：108个场景、94,176行数据和`QUALIFIED`模型均可由服务重新读取。
- `git diff --check`未发现空白错误；仅有Git提示未来可能按Windows设置转换换行符。
